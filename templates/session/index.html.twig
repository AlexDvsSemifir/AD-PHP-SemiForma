{% extends 'base.html.twig' %}

{% block title %}Session index{% endblock %}

{% block body %}
    <h1>Session index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Session</th>
                <th>Dates</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for session in sessions %}
            <tr>
                <td>{{ session.nom }}</td>
                <td>Du {{ session.dateDebut ? session.dateDebut|date('Y-m-d') : '' }} au {{ session.dateFin ? session.dateFin|date('Y-m-d') : '' }}</td>
                <td>
                    <a class='btn btn-primary' role='button' href="{{ path('app_session_show', {'id': session.id}) }}">Détail</a>
                    <a class='btn btn-primary' role='button' href="{{ path('app_session_edit', {'id': session.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class='btn btn-primary' role='button' href="{{ path('app_session_new') }}">Nouveau</a>
{% endblock %}
